# Card service -- staging environment (AWS). Commented out until re-enabled
# -- see docs/onboarding-new-app.md for the checklist to bring an app job live.

# Scrape CARD service - https://app.card-stage.adc.seattle.gov/api/metrics/
# - job_name: "card-service"
#   static_configs:
#     - targets: ["app.card-stage.adc.seattle.gov:443"]
#   scheme: https
#   authorization:
#     type: Bearer
#     # credentials_file: /tmp/hris_card_api_dev_metrics_token
#   tls_config:
#     insecure_skip_verify: true
#   metrics_path: /api/metrics
#   scrape_interval: 15s
